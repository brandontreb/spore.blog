{{ $tags := .Site.Taxonomies.tags }}
{{ $len := len $tags}}
{{ $count := 0}}
{
    "categories" : [
    {{ range $name, $taxonomy := $tags -}}    
      {{ $name | jsonify }}
      {{ $count = add $count 1 }}
      {{ if lt $count $len -}}
      ,
      {{ end -}}   
    {{ end -}}
    ]
}