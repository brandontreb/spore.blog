<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('./partials/head'); %>
  <script src="/scripts/simplemde.min.js"></script>
  <link rel="stylesheet" href="/styles/simplemde.min.css">
</head>

<body class="container">
  <header>
    <%- include('./partials/header'); %>
  </header>
  <main>
    <content>

      <p>
        <% if(redirect.frontMatter.slug) { %>
        <form method="post" action="/admin/pages/redirects<%- redirect.frontMatter.slug %>?_method=put" style="display:inline">
        <% } else { %>
        <form method="post" action="/admin/pages/redirects/new" style="display:inline">
        <% } %>
          <label for="title">From Path:</label>
          <input placeholder="/example" type="text" id="from" name="from" value="<%- redirect.frontMatter.slug %>">
          <label for="title">Redirect To:</label>
          <input placeholder="Full URL or relative path" type="text" id="to" name="to" value="<%- redirect.frontMatter.redirect %>">
          <br />
          <!-- Back Button-->
          <button type="button" id="backButton" class="button" onclick="window.location.href='/admin/pages'"><span class="arrow">←</span>Back</button>
          <% if(redirect.frontMatter.slug) { %>
          <!-- Update Button-->
          <button class="button-secondary" id="updateButton" class="button" type="submit">Update Redirect</button>
          <% } else { %>
          <!-- Create Button-->
          <button class="button-secondary" id="createButton" class="button" type="submit">Add Redirect</button>
          <% } %>     
          
        </form>
        <% if(redirect.frontMatter.slug) { %>
        <!-- Delete Button-->
        <form method="post" action="/admin/pages/<%- redirect.frontMatter.slug %>?_method=delete" style="display:inline" onsubmit="return confirm('Are your sure you want to delete this page?');">
          <button type="submit" id="deleteButton" class="button-danger">Delete Redirect</button>
        </form>
        <% } %>
      </p>

    </content>
  </main>
  <footer>
    <%- include('./partials/footer'); %>
  </footer>

</body>

</html>